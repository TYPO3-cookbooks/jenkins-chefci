driver_config:
  # for kitchen-dokken
  chef_version: <%= node['jenkins_chefci']['kitchen']['chef_version'] %>

provisioner:
  name: chef_zero
  # version for kitchen-vagrant and kitchen-docker
  require_chef_omnibus: <%= node['jenkins_chefci']['kitchen']['chef_version'] %>
